name: 0 - ARŞİV · Eski Kolay Güncelleme Kullanma

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - name: Bilgi
        run: |
          echo "Bu eski v4.48.12 güncelleme akışı arşivlenmiştir."
          echo "Yeni güncellemelerde 2 - Android APK Oluştur (Üretim) akışını kullanın."
